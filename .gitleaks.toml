# Gitleaks Configuration for E3 Rentals
# Explicit, minimal allowlist for synthetic test fixtures and documentation samples.

[extend]
useDefault = true

[allowlist]
description = "Reviewed synthetic test fixtures and historical documentation placeholders"
paths = [
    '''DEVELOPER_HANDOVER_BIBLE\.md''',
]
regexes = [
    '''re_123456789abcdef''',
    '''whsec_dev_default_test_secret''',
]
